name: Supabase Keep Alive - The Wild Oasis

on:
  schedule:
    - cron: '0 0,12 * * *' # tiap 2x sehari 00:00 UTC (07:00 WIB) - 12:00 UTC (19:00 WIB)
  workflow_dispatch: # kalau mau manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print datetime
        run: date -u

      - name: Curl request to Supabase
        run: |
          curl -s "${{ secrets.SUPABASE_URL }}/rest/v1/cabins?select=id&limit=1" \
            -H "apikey: ${{ secrets.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}"
